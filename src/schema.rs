table! {
    users (id) {
        id -> Int4,
        first_name -> Varchar,
        last_name -> Varchar,
        phone -> Varchar,
        email -> Varchar,
        password -> Varchar,
        perm -> Int2,
    }
}
